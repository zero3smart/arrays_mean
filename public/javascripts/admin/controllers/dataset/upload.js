angular.module('arraysApp')
    .controller('DatasetUploadCtrl', ['$scope', 'AuthService',
        function($scope, AuthService) {
            $scope.init = function() {
            }
        }
    ]);