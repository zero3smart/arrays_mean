angular.module('arraysApp')
    .controller('DatasetSettingsCtrl', ['$scope', 'AuthService',
        function($scope, AuthService) {
            $scope.init = function() {
            }
        }
    ]);