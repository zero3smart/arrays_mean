angular.module('arraysApp')
    .controller('DatasetViewsCtrl', ['$scope', 'AuthService',
        function($scope, AuthService) {
            $scope.init = function() {
            }
        }
    ]);