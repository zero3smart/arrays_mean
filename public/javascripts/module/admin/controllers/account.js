angular.module('arraysApp')
    .controller('accountCtrl', ['$scope', 'authentication',
        function($scope, authentication) {
            $scope.init = function() {
            }

            $scope.save = function() {

            }
        }
    ]);