angular.module('arraysApp')
    .controller('datasetSettingsCtrl', ['$scope', 'authentication',
        function($scope, authentication) {
            $scope.init = function() {
            }
        }
    ]);